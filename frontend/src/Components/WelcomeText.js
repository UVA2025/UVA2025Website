const WelcomeText = () => {

    return (
        <div>

            {/* <Box
                display="flex"
                sx={{ justifyContent: "center" }}      >
                <Typography><h1>{props.display}</h1></Typography>
            </Box> */}
        </div>

    );
};

export default WelcomeText;